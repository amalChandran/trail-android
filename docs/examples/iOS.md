# Swift examples moved to trail-ios

Swift has its own root package and example app in the sibling `trail-ios` checkout. Read `trail-ios/docs/examples/iOS.md` for the compiler-checked examples and `trail-ios/Examples/TrailPlayground/IntegrationExamples.swift` for their source.

The intended public location after GitHub publication is [amalChandran/trail-ios](https://github.com/amalChandran/trail-ios). That remote is not claimed live yet. Android examples remain [here](Android.md).
